package market

type AuthService interface {
	// Key() (key interface{}, err error)
	// Validate(token interface{}) error
	// UserID(token string) (UUID, error)
}

// type Token struct {
// 	UserID UUID
// }
